export default function Hello() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>Hello route</h1>
      <p>App Router dynamic path <code>/hello</code> — NodeBrowser Next subset.</p>
      <p>
        <a href="/">Home</a>
      </p>
    </main>
  );
}
